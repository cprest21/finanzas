<%= form_for(@account_user) do |f| %>
  <% if @account_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_user.errors.count, "error") %> prohibited this account_user from being saved:</h2>

      <ul>
      <% @account_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= collection_select(:account_user, :account_id, Account.all, :id, :title) %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= collection_select(:account_user, :user_id, User.all, :id, :email) %>
  </div>
  <div class="field">
    <%= f.label :permission_level %><br>
    <%= f.number_field :permission_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
